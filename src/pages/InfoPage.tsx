import ReleaseNotesPage from './ReleaseNotesPage'

/**
 * Historical entry point for the old overview page.
 * The current destination is the Release Notes hub.
 */
export default function InfoPage() {
  return <ReleaseNotesPage />
}

